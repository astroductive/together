Together's surface container. `solid` for app screens, `glass` for the frosted marketing aesthetic, `ink` for dark feature panels.

```jsx
<Card variant="solid" padding={24}>…</Card>
<Card variant="glass">Frosted marketing card</Card>
<Card variant="ink">Dark panel with white text</Card>
```
