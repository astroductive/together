Sidebar navigation row with a lucide icon and an active state (teal rail + tint).

```jsx
<NavItem icon={<LayoutDashboard size={19} />} active>Overview</NavItem>
<NavItem icon={<History size={19} />} dot>History</NavItem>
```
