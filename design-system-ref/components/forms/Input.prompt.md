Labeled text input with a teal focus ring, optional leading icon, hint and error states.

```jsx
<Input label="Email" placeholder="you@example.com" />
<Input label="Name" error="Required field" />
<Input icon={<Search size={15} />} placeholder="Search signs…" />
```
